# 노블메트릭 진행상황

> 이 파일은 작업 세션마다 최신 상태를 맨 위에 누적 기록한다. (컨텍스트 압축 대비용)
> 핵심 경로: 프로젝트 `C:\Users\asm04\OneDrive\바탕 화면\AI창업\novelmetric` (2026-07-20 사용자가 직접 이동, 이전 `C:\Users\asm04\novelmetric`은 폐기) · 라이브 https://novelmetric.vercel.app
> 계정/키: Vercel env에 전부 등록됨 (`vercel env ls --scope songminan90-2258s-projects`) — 토스 결제 2종, Gmail 발송 2종, Anthropic, Supabase(ref cnvezqmnxsmdrqhagmrq, "Ahnsongmin's Project"). Supabase 키는 Vercel에서 Sensitive라 pull 불가 — 데이터 확인은 대시보드로.
> 대기자 확인: https://supabase.com/dashboard/project/cnvezqmnxsmdrqhagmrq/editor → waitlist 테이블 (7/20 현재 8명)

---

## 2026-07-30 (오전) — 로그인 화면을 흔한 아이디/비밀번호 폼으로 재구성 + DB 컬럼 실제 반영

### 왜 바꿨나
사용자가 어제 만든 회원가입 화면이 마음에 들지 않는다며 **"행운프레시 사이트처럼"** 바꿔달라고 했다.
행운프레시(과일 위탁 공급처, 스냅샷 `바탕 화면\과일위탁\lf-login.yml`)의 로그인 화면 구조는:

```
로고 + 서비스명 / 한 줄 설명
아이디   [아이디를 입력하세요]
비밀번호 [비밀번호를 입력하세요]
[ ] 아이디 저장                비밀번호 찾기
[      로그인      ]
[ 입점 신청 / 부제 한 줄 ]
```

즉 **아이디·비밀번호를 라벨과 함께 한 화면에서 받는 가장 흔한 형태**다. 반면 우리 화면은
`password_hash` 컬럼이 없어서 비밀번호 폼이 스스로 숨은 상태였고, 그 결과 **"이메일 입력 →
로그인 링크 받기"(메일 링크)만 남아** 낯선 화면이 되어 있었다. 지적이 맞다.

### 적용 (커밋 `de07d17`, 그 앞에 안전망 스냅샷 `d61b937`)
- `components/LoginGate.tsx` 전면 재작성: 라벨 붙은 **이메일 / 비밀번호** 입력 → 그 아래 한 줄에
  **이메일 저장 체크박스 + 비밀번호 찾기** → **로그인** 버튼 → **회원가입** 버튼(부제 "이메일과
  비밀번호만 있으면 30초"). 가입 모드에서는 "이미 계정이 있어요 · 로그인"으로 되돌아간다.
- `/login`: 상단에 📈 노블메트릭 + "웹소설 노출·성장 추적 시스템", 카드 폭 `max-w-sm`으로 좁혀
  로그인 페이지답게. **기본 모드는 로그인**이고 가입 유도 링크만 `?mode=signup`으로 들어온다.
- **메일 링크는 "비밀번호 찾기" 자리로만 남았다** — 코드는 그대로 재사용(버린 것 없음).
- **이메일 저장**은 `localStorage.nm_login_email`. 비밀번호는 저장하지 않는다.
- 소셜 버튼은 폼 아래 "또는" 구분선 뒤로 내려갔다(구글 키가 등록되면 그때 나타남. 지금은 0개라
  화면상 행운프레시와 동일한 구성).
- 어제 넣어둔 임시 장치 **제거**(죽은 코드): `passwordLoginAvailable()`, `me.password`,
  `Me.password` — 컬럼이 실제로 생겼으니 존재 여부를 탐지할 이유가 없다.

### DB: `nm_users.password_hash` 실제 추가 완료
어제 못 넣은 컬럼을 넣었다. `nm_users` = `id · email · created_at · last_login_at · password_hash`(5개) 확인.

> **함정(다음에 시간 아끼려고)**: Supabase SQL Editor를 **`/sql/new`로 직접 열면 body가 계속 0**으로
> 멈춘다(어제 실패 원인). **`/editor`(Table Editor)를 먼저 로드한 뒤 왼쪽 메뉴에서 SQL Editor를
> 클릭**하면 정상적으로 뜬다. SQL 입력은 `window.monaco.editor.getModels()[0].setValue(sql)`.

### 프로덕션 검증 (배포 후 라이브 curl)
| 확인 | 결과 |
|---|---|
| `/login` 화면 (390px) | 새 폼 렌더, 가로 넘침 없음, 콘솔 에러 0 |
| `/api/auth/me` | `{"enabled":true,"email":null,"providers":[]}` — `password` 키 사라짐(새 코드 확인) |
| 비밀번호 회원가입 | 200 + 세션 쿠키, `/api/auth/me`에 이메일 반영 |
| 같은 계정 로그인 | 200 |
| 비밀번호 틀림 | 401 "이메일 또는 비밀번호가 맞지 않아요." |
| 이미 가입된 이메일로 재가입 | 409 "이미 가입된 이메일이에요." |
| 8자 미만 비밀번호 | 400 "비밀번호는 8자 이상으로 만들어 주세요." |

검증용 계정 `nm-verify-0730@novelmetric.test`가 `nm_users`에 남아 있다(지우려면
`delete from nm_users where email like 'nm-verify%';` — 삭제는 사용자 승인 후).

### 소셜 로그인 폐기 — 로그인 수단은 이메일+비밀번호 하나로 확정 (커밋 `124b0ec`, **미배포**)
사용자 결정: **"구글이나 그런 거 그냥 하지말고 지금으로만 유지하자."** 키가 들어오면 버튼이
자동으로 뜨던 장치는 이제 켜질 일이 없어 죽은 코드다 → 400줄 삭제.

- 삭제: `lib/oauth.ts`, `app/api/auth/oauth/**`, `docs/SOCIAL-LOGIN-SETUP.md`,
  `session-client`의 `ProviderId`·`PROVIDER_LABEL`·`Me.providers`, `/api/auth/me`의 `providers`,
  `LoginGate`의 소셜 버튼 블록, `/login`의 OAuth 전용 오류 문구 7종(남은 건 메일 링크 만료 하나).
- **비밀번호 찾기(메일 링크)는 유지** — 비밀번호를 잊었을 때의 유일한 복구 경로.
- 로컬 검증: 빌드 통과, `/login`에 email·password·checkbox + 버튼 3개(비밀번호 찾기·로그인·회원가입),
  소셜 문구 0건, 390px 가로 넘침 없음, 콘솔 에러 0.
- ⚠️ **배포 보류**: 아래 사유 참고. 라이브에는 아직 소셜 코드가 남아 있으나 **키가 없어 화면에는
  아무 영향이 없다**(버튼이 원래 안 뜬다).

### ⚠️ 같은 폴더에 다른 세션 작업분이 OneDrive로 내려오는 중 (09:32\~09:36)
작업 중 `app/dashboard/page.tsx`·`app/page.tsx`·`app/api/{diagnose,novel,gate}`·`lib/pass.ts`·
`app/pro/payapp/page.tsx`·`supabase/schema.sql`·신규 `components/GuestPassNotice.tsx`가
**내 편집이 아닌 경로로 계속 바뀌었다.** 정체는 *계정 없이 Pro 코드만 가진 손님의 진단 허용*
기능(`nm_diag_usage_code` 테이블 + 코드 기준 월 한도 + 가입 권유 안내).

- 그 사이 빌드가 타입 에러로 실패했는데 **원인은 내 변경이 아니라 파일이 덜 동기화된 상태**
  (같은 파일 안에서 부모가 넘기는 props 9개 vs 자식이 받는 8개). 동기화가 멎은 뒤 재빌드하면 통과.
- **`d61b937`(09:18)에는 이 기능이 없었다 → 오늘 09:20 배포에도 안 실렸고, 라이브에 없다.**
- 그래서 지금 `vercel --prod`를 하면 **이 미검증 기능까지 프로덕션에 올라간다** → 배포 보류.
  이 기능은 `nm_diag_usage_code` 테이블이 없으면 사용량을 0으로 세므로(진단은 막지 않음),
  배포 시 그 테이블(`supabase/schema.sql` 맨 아래 Phase 6)을 함께 만들어야 한다.
- 이 작업분은 **커밋하지 않고 그대로 뒀다**(내 커밋에는 내 파일만 담았다).

---

## 2026-07-30 — 로그인 수단 확장: 비밀번호 + 구글 (일반적인 회원가입 형태로)

### 왜 바꿨나
사용자 판단: "우리 목적은 돈 내는 사람을 만드는 것이니 일반적인 웹처럼 회원가입을 하자."
**매직링크가 결제를 막는 지점은 가입이 아니라 재방문이다** — 세션이 끊기거나 다른 기기에서
들어온 사람을 결제 직전에 메일함으로 보내버린다. 이 지적은 맞다.

### 확인한 사실 (공식 출처 대조 — 이게 순서를 결정했다)
| 플랫폼 | 결론 | 출처 |
|---|---|---|
| **구글** | `email`·`profile`·`openid`는 **비민감 scope라 검증 절차 불필요**, 바로 공개 가능 | [support.google.com/cloud/answer/13463073](https://support.google.com/cloud/answer/13463073) |
| **네이버** | 등록 시 기본 `개발중` 상태 — **검수 승인 전엔 등록한 테스트 아이디만 로그인됨. 검수 약 1주일** | 개발자센터 가이드 |
| **카카오** | 로그인 자체는 앱 등록만으로 되지만 **카카오계정(이메일) 동의항목은 비즈앱 전환 + 심사 필요** | [developers.kakao.com](https://developers.kakao.com/docs/ko/kakaologin/prerequisite) |

카카오의 이메일 제약이 결정적이다 — 우리 계정의 존재 이유가 "알림 보낼 주소"라 이메일을 못
받으면 가입 후 이메일을 또 입력받아야 해서 소셜 로그인의 장점이 사라진다.
→ **되는 것부터 켜고(비밀번호·구글), 카카오·네이버는 신청 후 승인 대기**로 정리.

### 구현
- **비밀번호**: Node 내장 `scrypt`만 사용(bcrypt/argon2 같은 네이티브 의존성 0). 저장 형식
  `scrypt$솔트$해시`. 최소 8자. `POST /api/auth/password {mode:"signup"|"login"}`.
- **구글 OAuth**: 라이브러리 없이 표준 흐름 직접 구현(`lib/oauth.ts`).
  `/api/auth/oauth/[provider]` → 인가, `/api/auth/oauth/[provider]/callback` → 토큰 교환.
  CSRF는 서명된 state를 쿠키로 왕복. `id_token` 페이로드에서 이메일을 꺼내고
  **`email_verified`가 false면 계정을 만들지 않는다**(남의 주소 주장 방지).
  엔드포인트는 구글 OpenID 디스커버리 문서에서 확인한 값.
- **계정은 이메일 하나로 통일**: `nm_users.email`이 unique라 구글·비밀번호·메일링크 중 무엇으로
  들어와도 같은 주소면 같은 계정. 구글로 먼저 쓰던 사람이 비밀번호를 만들면 기존 계정에 붙는다.
- **매직링크는 유지**(사용자 결정) — 로그인 수단 겸 "비밀번호 찾기"로 쓴다. 버린 코드 없음.
- **가입 직후 하던 일을 이어서 한다**: 진단 폼에서 가입하면 입력해둔 제목으로 진단이 자동
  재실행되고, 추적 카드에서 가입하면 그 작품 추적이 바로 등록된다(`onSuccess` → `onLoggedIn`).
- `app/api/auth/oauth/` 아래로 소셜 라우트를 몰아넣었다 — `[provider]`를 `/api/auth/` 직속에
  두면 `request`·`me` 같은 정적 라우트와 그림자가 겹쳐 헷갈린다.

### 두 가지 안전장치 (Supabase 대시보드가 멈춰서 필요해진 것)
`password_hash` 컬럼을 넣으려 했는데 **Supabase SQL Editor가 끝까지 렌더되지 않았다**(body 길이 0,
새 탭·재접속 모두 실패). 대시보드가 회복되면 SQL 한 줄이면 끝나는데, 그 사이에도 라이브가
멀쩡하도록 두 가지를 넣었다.
1. **고아 계정이 안 생긴다**: 신규 가입은 계정 생성과 비밀번호 저장을 **한 번의 insert**로 한다.
   컬럼이 없으면 insert 자체가 실패해 계정이 만들어지지 않는다(두 단계로 나누면 비밀번호 없는
   계정이 남는다).
2. **비밀번호 폼이 스스로 숨는다**: `/api/auth/me`가 `password: boolean`을 내려준다
   (`passwordLoginAvailable()`이 컬럼 존재를 확인). 컬럼이 없으면 로그인 카드에 메일 링크만
   보이고, **SQL을 실행하는 순간 자동으로 비밀번호 폼이 나타난다.** 소셜 버튼도 같은 방식으로
   키가 등록되면 알아서 뜬다(`enabledProviders()`).

### 검증 (라이브)
- 빌드·타입체크 통과. 사용자 노출 문구 벤더명 0건(진단 결과는 "특화 AI 정밀 진단"으로 표시).
- `GET /api/auth/me` → `{"enabled":true,"email":null,"providers":[],"password":false}`
  → 의도대로 비밀번호 폼·소셜 버튼이 숨는 상태.
- `GET /api/auth/oauth/google` → 302 `/login?error=provider` (키 미등록 시 안전하게 되돌림)
- `POST /api/auth/password` 가입 시도 → **500, 계정 미생성** (컬럼 없음 → insert 전체 실패)
  ※ 계정이 안 만들어진 것은 코드 구조상 보장되지만 DB로 직접 확인은 못 함(대시보드 접근 불가).

### 남은 일
1. **SQL 한 줄** — `alter table public.nm_users add column if not exists password_hash text;`
   (+ 테스트 데이터 정리 `delete from tracked_novels where anon_id is not null; delete from nm_diag_usage;`)
   실행하면 비밀번호 로그인이 즉시 켜진다. 재배포도 필요 없다.
2. **구글 OAuth 클라이언트 생성** — `docs/SOCIAL-LOGIN-SETUP.md` §1 클릭 단위 절차.
   Claude가 하지 않은 이유: 구글 계정 설정 변경 + client secret 취급은 Claude 금지 영역.
   `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`을 Vercel에 넣고 재배포하면 버튼이 뜬다.
   Redirect URI는 정확히 `https://novelmetric.vercel.app/api/auth/oauth/google/callback`.
3. 카카오 비즈앱 전환 신청 / 네이버 검수 신청 — `docs/SOCIAL-LOGIN-SETUP.md` §2·§3.
   승인되면 `lib/oauth.ts`의 `PROVIDERS`에 항목 하나 추가로 끝난다.
4. 진단 모델 비교(아래 7/29 항목) — 여전히 `ANTHROPIC_API_KEY` 대기.

### 별도로 기억해 둘 것
결제 전환의 진짜 병목은 로그인보다 **Pro 기능이 "매주 오는 메일"이라 즉시 체감이 안 되는 것**일
가능성이 크다. 가입 흐름을 손봤으니 다음은 Pro의 즉시 체감 요소를 볼 차례다.

---

## 2026-07-29 (저녁) — 회원가입(매직링크) 도입 + 무료 한도 우회 구멍 차단

### 왜 했나 — 신원이 없어서 두 군데서 새고 있었다
1. **추적 무료 1작품 한도가 통째로 우회됐다.** `app/api/novel/route.ts`의 한도 검사가 `body.contact`가 있을 때만 돌아서, "알림 안 받음"을 고르면 검사 자체를 건너뛰었다. 무제한 등록 가능.
2. **진단 무료 횟수가 시크릿창으로 무한 반복됐다.** 쿠키(`nm_diag`) 기준이라 계정 개념이 없었다. 진단은 호출마다 실제 AI 비용이 나가는 유일한 기능이라 이건 돈이 새는 구멍.

### 확정된 게이트 (사용자 결정)
| 기능 | 게이트 |
|---|---|
| 대시보드 작품 추적 | **가입 없이 1작품** (`nm_anon` 서명 쿠키로 카운트) |
| 이메일 알림 등록 | **회원가입 필요** ← 설계의 핵심. 어차피 이메일을 치려던 참이라 마찰 0 |
| 제목 진단 | **회원가입 필요 · 계정당 매달 1회** (`FREE_DIAG_PER_MONTH` 3 → 1) |
| 추적 무제한 / 진단 월 30회 / 경쟁작 워치 / 장르 벤치마크 | Pro 9,900원/30일 |

가입 방식은 **이메일 매직링크**(비밀번호 없음, 15분·1회용 토큰). 새 의존성 0 — `lib/pass.ts`의 HMAC 서명 쿠키 패턴과 `lib/notify.ts`의 `sendEmail`(Gmail SMTP)을 그대로 재사용.

### 신설·변경
- **DB(적용 완료)**: `nm_users` / `nm_login_tokens`(sha256 해시만 저장) / `nm_diag_usage`(user_id+ym) 신설, `tracked_novels.user_id`·`anon_id`, `nm_pass.user_id` 추가, partial unique index 2개(`tracked_by_user_idx`·`tracked_by_anon_idx`).
- **`lib/auth.ts`(신설)**: 세션/익명 서명 쿠키, 토큰 발급·1회 소비, 계정 기준 진단 사용량, `adoptTracking`(로그인 시 기존 추적 승계 — 레거시 이메일 행 + 이번 브라우저의 익명 행).
- **`app/api/auth/{request,callback,logout,me}`** 신설, **`/login`** 페이지 신설.
- **게이트**: `/api/diagnose`는 비로그인이면 **AI 호출 전에 401**. `/api/novel` POST는 한도를 `body.contact` 유무가 아니라 **소유자(user_id/anon_id) 기준으로 항상** 검사 → 1번 구멍 근본 해결. 알림 채널을 켜는데 비로그인이면 401.
- **`lib/db.ts`**: `trackNovel`이 소유자 기준으로 조회 후 갱신(partial index는 PostgREST가 onConflict 추론을 못 해 upsert를 못 쓴다). 레거시 `email` 컬럼에는 더 이상 쓰지 않는다(옛 unique(novel_id,email) 충돌 회피). `listWeeklyTargets`는 `pass_code` 또는 **계정 귀속 패스** 둘 다 인정.
- **Pro 패스가 계정에 붙는다**: 로그인 상태로 코드를 적용/결제하면 `nm_pass.user_id` 연결 → 기기를 바꾸거나 코드를 잃어도 유지.
- **프론트**: `components/LoginGate.tsx`(401 자리에 그대로 끼우는 가입 카드), `components/AuthBadge.tsx`, `lib/session-client.ts`(4곳에 복붙돼 있던 localStorage `nm_pass` 파싱 통합). 대시보드는 비로그인이면 알림 채널 기본값을 "알림 안 받음"으로 내리고, 이메일을 고르는 순간 LoginGate로 바뀐다.
- **카피**: "가입 없이"를 **추적에만** 남기고 진단에서 제거. "월 3회" → "매달 1회". `/pro`의 "회원가입 없이…" 재작성. `/result`의 "대안 제목 5개"(실제 3개) 수정.
- **계측**: `signup_request` / `signup_done` / `login` / `diagnose_401` / `track_401` 추가.

### 검증 (라이브, 전부 실측)
- 빌드·타입체크 통과. lint 에러 18건은 **전부 기존 코드의 `<a>` 네비게이션/`setState-in-effect` 패턴**(not-found·guide·insights·compare 등 미수정 파일 포함) — 이번 변경으로 새로 생긴 건 0건.
- `GET /api/gate` → `{"enabled":true,"authRequired":true,...}` (게이트 켜짐 확인)
- 비로그인 `POST /api/diagnose` → **401 LOGIN_REQUIRED**
- 비로그인 `POST /api/novel {channel:"email"}` → **401 LOGIN_REQUIRED**
- 익명 `{channel:"none"}` 1건째 → 200 + `nm_anon` 쿠키 발급 / **다른 작품 2건째 → 402** ← 우회 구멍 막힌 것 확인
- **E2E**: `/api/auth/request` → Gmail IMAP으로 실제 메일 수신 확인 → 콜백 302 + `nm_session` 발급 → `/api/auth/me` = songminan90@gmail.com → 진단 1회 200(engine=claude) → **2회째 402** → `freeLeft` 1→0.

### 남은 일
1. **진단 모델 비교 미실행 — 사용자 조치 필요.** `scripts/compare-diagnose-models.mjs`는 `ANTHROPIC_API_KEY`가 있어야 도는데, **Vercel이 사용자 정의 env 값을 전부 `""`로 마스킹해 pull이 불가**(전 항목 value-len=2 확인). `.env.local`에 키 한 줄을 직접 넣어야 실행 가능. 확인된 사실: Sonnet 4.6 $3/$15 vs Haiku 4.5 $1/$5 = 정확히 3배. 단 `SYSTEM_RUBRIC`이 1,000~1,200토큰이라 **Haiku의 캐시 최소치(4,096)에 못 미쳐 프롬프트 캐시가 무효**가 된다(입력은 비용의 15%라 영향은 작음). 교체 시 `DIAGNOSE_MODEL=claude-haiku-4-5` env 추가 + `lib/diagnose.ts`의 `cache_control` 제거.
2. **검증용 테스트 데이터 정리**: 익명 추적 1행(작품 480433)과 내 계정의 이번 달 진단 사용량 1회가 남아 있다. Supabase SQL Editor에서
   `delete from tracked_novels where anon_id is not null; delete from nm_diag_usage;`
   (SQL Editor가 응답이 느려 이번 세션에서는 실행하지 못함)
3. 변경사항은 **커밋 안 됨**(배포는 로컬 디렉터리 기준이라 라이브에는 반영됨).
4. 8/3 홍보 후 `select event, count(*) from nm_events where created_at > now() - interval '7 days' group by event;`로 가입 퍼널 이탈 지점 확인.

---

## 2026-07-29 — 주력 상품을 '제목 진단' → '작품 추적'으로 전환

### 판단 근거 (사용자 전제는 틀렸고 결론은 맞았던 건)
- 사용자 관찰: "방문자가 대시보드에 가장 많이 온다 = 제목 진단은 안 쓰인다."
- **조사 결과 전제가 틀림**: 진단은 별도 라우트 없이 홈 히어로에 폼째로 임베드돼 있어(`app/page.tsx`) **페이지뷰 자체가 안 찍힌다.** 반면 7/27 아카 런치 때 크레딧 $5.50이 진단 실시간 호출로 하루 만에 소진 — 호출당 약 $0.025로 역산하면 **200~250회**(토큰 추정, 실측 아님). 이용률은 오히려 높았다.
- **그럼에도 전환은 옳다**: Claude를 호출하는 코드는 프로젝트 전체에 `lib/diagnose.ts`(방문자가 누를 때마다 실시간)와 `lib/curiosity.ts`(크론 하루 2회, ~$0.03/일) **둘뿐**. 즉 진단은 쓸수록 원가가 비례해 늘고, **추적·대시보드는 Claude를 아예 안 써서** 크론 1회를 전원이 나눠 쓴다. 재방문 이유도 추적에만 있고(매일 새벽 갱신), 이메일 확보도 추적 7명 vs 진단 캡처 0명(7/25).
- **결론**: 진단 = 가입 없이 쓰는 무료 미끼(비용 상한 씌움), 추적 = 수익 축. 제품 실체는 이미 추적 중심인데 랜딩만 진단 중심이었음.

### 작업 1 — 퍼널 계측 신설 (가장 중요)
- **문제**: 진단 실행 수를 확인할 방법이 전무했음. `/api/diagnose`에 DB 쓰기 0줄, 흔적은 브라우저 쿠키(`nm_diag`)와 Vercel 로그(Hobby 1시간 보존)뿐. 반면 추적은 `tracked_novels.created_at`으로 소급 확인 가능 → **비대칭이라 비교 자체가 불가능했다.**
- `supabase/schema.sql`에 **`nm_events`** 테이블 추가(event/meta jsonb/created_at). 개인정보 미저장, 집계용 메타만.
- `lib/db.ts`에 `logEvent(event, meta)` — DB 없으면 no-op, 실패해도 요청을 안 깨뜨림.
- 기록 지점 6종: `diagnose_run` · `diagnose_402` · `track_add` · `track_402` · `pro_view`(gate에 `?from=pro`) · `pro_paid`(토스/페이앱 양쪽).
- `track_add`에 `fromDiagnose` 포함 → 진단→추적 퍼널 전환율 측정 가능.
- **⚠️ 배포 전 Supabase SQL Editor에서 `nm_events` 생성 필요** (미생성 시 계측만 조용히 실패, 서비스는 정상).

### 작업 2 — 랜딩·Pro 재배치 (진단 유지 + 추적으로 연결)
- 사용자 확정: **히어로 진단 폼은 유지**(0마찰 유입 장치로 재대익), 메시지만 추적 중심으로.
- H1 "제목부터 데이터로 점검하세요" → **"매일 데이터로 확인하세요"**, 서브카피에 4개 플랫폼·매일 자동 집계 명시. 폼 위에 "1단계 · 제목 클릭률 무료 진단", 폼 아래에 "이미 연재 중이라면 작품 추적부터 →".
- **진단 결과 하단 1순위 CTA를 `TrackCta`로 신설** — 기존 1순위였던 이메일 캡처(source=diagnose, 실적 0명)를 2순위로 내림. 제목을 `?q=`로 넘겨 대시보드에서 **자동 검색**까지 실행(`from=diagnose`는 계측용).
- **사실과 다른 문구 2건 수정**: FAQ "문피아·노벨피아만 지원"(실제 4개 플랫폼) / "매일 자동 추적·알림은 추후 Pro로 제공 예정"(이미 구현됨). `/pro/success`·`/pro/payapp`의 "모든 기능 무제한"도 수정.
- ValueProps·Roadmap 순서를 추적 우선으로, FinalCta 버튼 "무료로 분석 시작" → "무료로 작품 추적 시작".

### 작업 3 — 진단 비용 구조 (호출당 $0.025 중 85%가 출력 토큰)
- **출력 축소(적용)**: 대안 제목 5개 → **3개**(`TITLE_SUGGESTION_COUNT`), reason 한 문장 제한, `max_tokens` 1500 → **900**. 휴리스틱 폴백도 동일 개수로 slice.
- **Pro 상한(적용)**: `PRO_DIAG_PER_MONTH = 30` 신설. 기존 "Pro=진단 무제한"은 헤비 유저 한 명이 9,900원 마진을 넘겨 쓸 수 있는 구멍이었음. 402 응답을 `PRO_REQUIRED`(무료 소진, Pro 링크 노출) / `LIMIT_REACHED`(Pro 상한)로 분리.
- **모델 교체(보류 — 사용자 결정 대기)**: `DIAGNOSE_MODEL` env로 분리(기본값 `claude-sonnet-4-6` 유지). `scripts/compare-diagnose-models.mjs` 신설 — 같은 제목 5개를 Sonnet 4.6 vs Haiku 4.5에 넣어 품질·실측 비용을 나란히 출력. **실행하려면 `.env.local`에 `ANTHROPIC_API_KEY` 한 줄 필요(사용자가 직접 입력)**. Haiku는 정확히 1/3 단가($1/$5 vs $3/$15).

### 곁가지로 발견·수정한 모바일 버그 (기존 문제)
- **390px에서 대시보드·베스트·비교 페이지 전체가 가로로 넘쳤음.** 원인: `<main className="mx-auto max-w-4xl flex-1">`에서 **`mx-auto`(auto 마진)가 flex 자식의 stretch를 무효화**해 main이 콘텐츠 폭(fit-content, 462px)이 됨. `w-full` 추가로 해결(3개 파일).
- H1·CTA에 `break-keep` 추가 — 한국어 줄바꿈에서 "요." 같은 고아 글자 방지.

### 검증 (전부 통과)
- `tsc --noEmit` 통과 / `next build` 통과(27개 라우트) / eslint 18건은 전부 기존 코드 패턴(`no-html-link-for-pages` 14, `set-state-in-effect` 4). 이번에 추가된 건 대시보드 신규 useEffect 1건뿐이며 기존과 동일 패턴.
- 390px 실측: 진단 실행 → 결과 하단 CTA 1순위 렌더 확인 → 클릭 시 `/dashboard?from=diagnose&q=...`로 이동 → **검색창 프리필 + 자동 검색까지 정상 동작 확인**. 콘솔 에러 0·경고 0, 가로 넘침 0(scrollWidth 375 = viewport).
- 벤더명 grep: 사용자 노출 문구 0건(코드 내 `"claude"`는 engine 타입 리터럴, 화면 표기는 "특화 AI 정밀 진단").

### 배포·계측 검증 완료 (19:5x KST)
- `npx vercel --prod --yes` 배포 완료(git remote 없음 — CLI 배포만 가능). 라이브 확인: 새 히어로 카피 반영, `/api/gate`에 `diagLimit:3` 신규 필드, 진단 호출 시 `engine:claude` + 대안 제목 3개.
- **Supabase `nm_events` 테이블 생성 완료** — claude-in-chrome으로 SQL Editor에서 직접 실행("Success. No rows returned").
  - 주의: 대시보드 최초 접속 시 수십 초간 body가 비어 스크린샷이 타임아웃되지만 **렌더 실패가 아니라 로딩 지연**. 탭 제목이 "SQL Editor | …"로 바뀌면 정상. SQL 입력은 Monaco라 `window.monaco.editor.getModels()[0].setValue(sql)`가 확실하고 실행은 Run 버튼 클릭.
- **적재 검증**: 라이브에서 `/api/gate?from=pro` + 진단 1회 호출 → `nm_events`에 `pro_view`(meta `{"hasPass":false}`), `diagnose_run`(meta `{"pro":false,"genre":"판타지","engine":"claude","hasSynopsis":…}`) 각 1행 확인. 제목·이메일 등 개인정보는 저장되지 않음(설계대로).

### 다음 할 일
1. **8/3 회원가입 시스템(최우선)** — 그때 "가입 없이 즉시" 카피를 한 번 더 손봐야 함(진단만 가입 없이 / 추적은 가입).
2. `compare-diagnose-models.mjs` 실행(`.env.local`에 `ANTHROPIC_API_KEY` 한 줄 필요, 사용자가 직접 입력) → 결과 보고 Haiku 교체 여부 결정. 교체 시 Vercel env에 `DIAGNOSE_MODEL=claude-haiku-4-5`만 추가(코드 수정 불필요).
3. 추적 무료 1개 한도 우회(`channel:none`이면 이메일이 없어 검사 스킵)는 회원가입에서 근본 해결.
4. 8/3 홍보 후 `select event, count(*) from nm_events where created_at > now() - interval '7 days' group by event;`로 진단 vs 추적 실사용 비교 → 주력 전환 판단이 맞았는지 검증.

## 2026-07-17

### 현재 상태
- **개발·배포·수익화 인프라 전부 가동 중.** 남은 병목은 홍보(사용자 유입)뿐.
- 프로덕션 = 최신 커밋 `c78a1d1` (오늘 05:19 배포, Ready 확인).
- 매일 크론(`/api/cron/collect`, UTC 18시 = KST 새벽 3시) 정상 — `/insights`에 2026-07-17 리포트 존재 확인.
- Pro 패스 결제(토스, 9,900원/30일) 켜져 있음. 로그인 없는 NM-코드 방식.
- 작업 트리 깨끗함 (커밋 안 된 변경 없음).

### 최근 완료 (7/16~17)
- Pro 기능 2종: 주간 성장 리포트 이메일 + 대시보드 심화 추이 분석 (`c78a1d1`)
- 카카오 알림 선택지 "준비 중" 비활성화 — 정직성 수정 (`092ce7e`)
- 연독률 계산 커뮤니티 통용 기준 보정 + 이정표·CSV (`51632fe`)
- Gmail SMTP 폴백 + 대기자 안내 발송 스크립트(dry_run 기본) (`b62be2b`)
- 그 이전: 노벨피아 플랫폼 지원, 베스트 Top100 + 문피아/노벨피아 분리 탭, 문피아 신규 JSON API 대응, 작가 기능 3종(연참 리듬·선작 전환율·투베 도달 예상일)

### 방문자 (Vercel Analytics, 7/10~7/17 최근 7일 — 7/17 22시 확인)
- 방문자 185명 · 페이지뷰 578 · 이탈률 43% · 한국 98% · 모바일 69%
- 페이지별: 홈 120 · /dashboard 85 · /best 71 · /insights 39 · /pro 13
- 리퍼러 거의 없음(직접 유입 위주) → 아직 홍보 전이라 예상 범위

### 홍보 진행 (7/17)
- **아카라이브: 게시 완료** (사용자가 직접 올림)
- **디시 웹연갤(id=tgijjdd): 무단 게시 금지 확인** — 매니저 공지 규정상 광고글은 메일(thisday753159@gmail.com) 또는 신문고로 사전 문의 필수. 유료 모델 포함 광고는 1회만 허용. 문의 메일 초안·게시 글 초안·게시 방법을 LAUNCH.md §A-2에 정리해 둠. → 사용자가 메일 발송 → 허가 후 게시.

### 디시 매니저 문의 (7/17 저녁)
- **문의 메일 발송 완료** (사용자가 직접 songminan90@gmail.com → thisday753159@gmail.com, "노블메트릭 드림" 서명).
- **답장 자동 감시 가동**: 20분마다 `node scripts/check-manager-reply.mjs` (IMAP 조회, 처리 기록 scripts/.manager-reply-state.json). 답장 오면 푸시 알림 + 정책대로 자동 회신(`scripts/send-reply-email.mjs`). 회신 정책: 허가→글 초안 검토 요청 / 질문→사실만 답변 / 거절→감사 인사만 / 애매→사용자에게만 알림.
- **한계**: 감시 예약은 Claude Code 세션이 켜져 있는 동안만 동작(세션 종료 시 소멸, 최대 7일). 새 세션에서 "매니저 메일 감시 다시 켜줘"라고 하면 위 스크립트로 즉시 재가동 가능.

### 디시 매니저 답장 — 게시 허가 완료! (7/17 23:05 수신, 자동 처리)
- 매니저 답장 요지: "게시글 서두에 허락받았다고만 적고 **자유롭게 업로드**하면 됨. 본인도 신작 때 이용해 보겠다."
- 자동 감사 회신 발송 완료 (23시대, messageId ffbc2a7d…, 서두 명시 약속 + 피드백 환영 인사).
- 게시용 글 최종본: LAUNCH.md §A-2 ② — 서두에 "(매니저님께 허락받고 올리는 글임)" 반영 완료.

## 2026-07-20

### 제목 '궁금증 지수' 기능 구현 (아카 댓글 약속 이행, 16:40 KST)
- 배경: 아카 글(177401493)의 unknown 댓글("1층 안올라감은 왜?가 먼저 떠서 1화 눌러볼 제목")에 "다음 집계 때 제목이 질문을 유발하는가를 분류해 보겠다"고 공개 답글 → 기능으로 구현.
- **lib/curiosity.ts** (신규): 제목 배열 → 0~100 궁금증(호기심 갭) 점수. ANTHROPIC_API_KEY 있으면 Claude 배치 채점(플랫폼별 1회 호출), 없으면 휴리스틱 폴백. diagnose.ts와 동일한 env-gate 패턴.
- **수집 크론**(app/api/cron/collect)에서 매일 문피아·노벨피아 제목 채점 후 questionScore를 nm_best_daily에 저장(스키마 변경 불필요, JSON 컬럼). 채점 실패해도 수집은 계속.
- **lib/analyze.ts**: analyzeBest에 curiosity 블록(상위 25% vs 하위 25% 평균 조회수). 점수 없는 과거 데이터는 null → 페이지에 카드 안 뜸.
- **/insights/[day]**: "🤔 제목 궁금증 지수" 카드. 사용자 노출 문구는 "AI 채점"만(벤더명 없음).
- 검증: tsc·eslint·next build 통과. 휴리스틱 실측에서 "1층 안올라감" 72점 최상위, "SSS급 회귀 천재 먼치킨" 45점 하위 — 의도 방향 일치.
- **주의**: 프로덕션에 CRON_SECRET 미설정 → /api/cron/collect가 인증 없이 호출 가능(기존 상태). 추후 secret 추가 권장.
- 다음 아카 글 소재: "지난주 예고한 질문형 제목 분류, 데이터로 이렇게 나왔습니다" (점수 쌓인 뒤).

### ✅ API 키 계정 불일치 발견·해결 (17:30 KST) — 근본 원인은 크레딧이 아니라 '엉뚱한 계정 키'
- 증상: 크론 로그에 `credit balance is too low`(org `9395646b...`). 그런데 사용자 콘솔(Songmin's Individual Org, songminan90@gmail.com)엔 $4.96 크레딧·$1,000 한도 멀쩡.
- **진단**: Vercel에 46일 전 등록된 키가 이 funded 계정이 아니라 잔액 0인 다른 조직 소속. 그 계정 API 키 2개(songmin-api-key 7/13, C-project 5/8) 모두 "최근 사용" 흔적 없음 = 매일 도는 Vercel 크론이 여기 키를 안 쓴다는 증거. → 결론: 키가 다른 조직에 있었음.
- **영향(과거)**: 이 때문에 제목 정밀 진단(/api/diagnose)도 그동안 계속 "샘플 진단(데모)" 폴백으로 돌고 있었음.
- **해결**: funded 계정에서 새 키 발급 → 사용자가 Vercel 대시보드에서 ANTHROPIC_API_KEY 교체(Production) → 재배포(rn9dglsyi). **API 키·비밀값 입력은 Claude 금지 규칙이라 사용자 직접 수행**(브라우저로 페이지만 띄워줌).
- **검증 완료**: /api/diagnose → `engine:claude` 복귀 확인. 크론 재실행 후 /insights/2026-07-20에 궁금증 카드 정상 렌더(문피아 상위25% 780,901 vs 하위25% 796,103, 10작 기준).
- **주의(보안)**: 사용자가 채팅창에 키 2개 평문 노출 → 서비스 안정화 후 콘솔에서 해당 키 revoke하고 새로 발급 교체 권장.
- **관찰**: 오늘 표본에선 궁금증 상위(780,901)가 하위(796,103)보다 오히려 약간 낮음 = 상관 약함/방향 불명확. 하루치라 결론 금물. 며칠 쌓인 뒤 다음 아카 글에 "질문형 제목 실제로 조회수 붙나" 정직하게 다룰 것.

### 페이앱 결제 연동 완료·배포 (7/23 낮) — 결제 전자동화
- **toss.me(개인 송금) 2024-08 종료 확인** → 플랜B 폐기. **페이앱(PayApp)으로 확정**: 가입비 0(이벤트)·개인(비사업자) 가입 가능·비사업자 수수료 4%·결제받기는 가입 즉시, 서류심사(3~4영업일)는 정산 전까지만.
- 사용자가 페이앱 가입(아이디 novelmetric) + Vercel에 PAYAPP_USERID/LINKKEY/LINKVAL 등록(직접).
- 구현: `lib/payapp.ts`(payrequest→payurl, HMAC 서명 var2) + `/api/payapp/checkout`(폰번호→결제창) + `/api/payapp/feedback`(웹훅: pay_state=4·서명·금액 검증→createPass 자동발급, 중복방지) + `/api/payapp/code`(폴링) + `/pro/payapp`(코드 표시). /pro에 폰입력+카드결제 버튼. passEnabled에 페이앱 포함.
- **보안**: 토스 테스트 키 prod 제거(테스트 결제로 진짜 코드 발급되던 구멍 차단), GATE_SECRET 등록(진단 쿠키 서명).
- 라이브 검증: gate `payapp:true, enabled:true, freeLeft:3` / 가짜 웹훅 400 거부 / 폰번호 검증 400 / 빌드·tsc·lint 통과.
- **남은 검증**: 실결제 E2E 1건(사장님 카드 9,900원 → 코드 자동발급 → 페이앱 관리자에서 취소). 계약서류 제출(정산용).
- 주의(보안): 페이앱 로그인 비번·연동키가 채팅에 노출됨 — 비번 변경 권고했고, 연동키는 추후 재발급+Vercel 교체 권장.

### 월요일(7/27) 출시 결정 + 준비 (7/23)
- **결정**: 다음주 월요일 아카 글 + 대기자 메일 + Pro 베타 동시 출시.
- **결제 = 플랜 B(개인 토스 송금, 비용 0원)**. 이유: 토스페이먼츠 정식 결제는 가입비 22만+연 11만(공식 확인)이라 검증 전 과투자. 냠냠상회(사업자 4220103844) 폐업 예정이라 사업자 심사도 월요일까지 불가. 로드맵: 반응 오면 페이앱(개인 가입·가입비0·카드결제) → 사업자 내면 네이버페이(연회비0)+포트원. **토스는 스킵 가능**.
- **준비 완료**: 대기자 10명 명단 갱신(recipients.txt, gitignore). 체험코드 10개 발급기 `scripts/gen-pass-codes.mjs`(만료 2026-09-30, `--one`으로 송금고객 1건 발급). 코드/SQL=`waitlist-codes.json/.sql`(gitignore). 메일 `waitlist-message.txt`에 Pro 체험코드 안내 추가 + 발송스크립트가 {{CODE}} 개인 삽입(dry_run 검증됨). 런북 `scripts/LAUNCH-RUNBOOK-0727.md`.
- **남은 것**: ①체험코드 10개 DB 삽입(waitlist-codes.sql — 브라우저 불안정으로 보류, 월요일 전 실행) ②사장님 개인 토스 송금 링크 받기 ③/pro 플랜B 배포(송금링크+코드안내로 결제버튼 교체) ④아카 글 원고.
- 대기자 메일 실발송은 `--live`, 공개라 사장님 최종확인 후.

### 홍보 UTM 측정 세팅 (7/22 자정)
- 문제: 홍보 링크에 UTM 없어 Vercel Analytics에서 전부 "직접 유입" → 어느 커뮤니티·글이 효과인지 구분 불가.
- 해결: `scripts/utm.mjs` 생성기 + `scripts/utm-links.md` 레퍼런스. `node scripts/utm.mjs <캠페인>`로 채널(arca/dcinside/munpia/novelpia)×도착지(/insights,홈)별 태그 링크 출력.
- 규칙: source=커뮤니티, medium=post/comment, campaign=글마다 새로(비교용). 읽기: Analytics 하단 "UTM Parameters" 탭.
- 검증: 태그 링크 home/insights 둘 다 http 200, @vercel/analytics는 layout.tsx에 연결됨 → UTM 자동 캡처.
- 다음 홍보(궁금증 후속 글)부터 campaign=curiosity-0722 링크 사용. 코드 배포 불필요(링크만 태그).

### 방문자→이메일 전환 개선 A+B 배포·검증 (7/21)
- 배경: 최근 7일 방문 354명인데 이메일 남긴 사람 14명(대기자 8 + 대시보드 추적알림 6). 트래픽 대비 전환 얇음.
- 이메일 저장은 2군데: waitlist 테이블(홈 하단 폼, source 구분) + tracked_novels(대시보드 추적 시 이메일 알림 켠 사람). 두 그룹 안 겹쳐 8+6=14.
- **A**: 홈 진단 결과(ResultView) 하단에 "📮 매주 잘 팔리는 제목 트렌드 이메일" 캡처 박스 추가 → /api/waitlist에 source:"diagnose"로 저장(전환 지점 구분 가능). 기존 11px 각주 링크만 있던 자리.
- **B**: 대시보드 추적 박스 기본 채널 none→email(useState), 이메일 버튼 맨 앞 배치, "매일 접속 안 해도 이메일로" 문구. "알림 안 받음"은 유지(강요 아님).
- 정직성: 거짓주장 없음·옵트아웃 유지·숨은결제 없음·벤더명 없음. tsc/build 통과(eslint 5건은 기존 코드 사전존재 경고, 무관).
- 라이브 검증: 홈 진단 시 이메일 박스 렌더 확인(AI 정밀진단도 정상), 대시보드 추적박스 이메일 기본선택+입력창 열림 확인.
- 다음: source별 전환 수 며칠 뒤 비교(diagnose vs landing vs tracked). 홍보 링크 UTM은 아직 미적용(보류).

### 궁금증 지수 데이터 2일치 판독 + 카드 정직화(A+C 방침, 7/21)
- 데이터(문피아만, 노벨피아는 조회수 미제공이라 카드 없음): 7/20 상위 780,901 vs 하위 796,103(평평) / 7/21 상위 1,443,496 vs 하위 535,212(2.7배). **이틀 정반대 = 노이즈.**
- 근본 한계: 문피아 목록은 TOP10만 조회수 제공 → "10작 채점 기준", 상·하위 25% = 각 2작(2 vs 2). 게다가 조회수=순위와 교란(궁금증 상위≈투베 최상위).
- 정성 관찰(이건 사실): 아카 댓글의 세 제목(1층 안올라감/소소 힐링/아르헨티나)이 7/21 투베 1·2·3위, 7/20 2·4·5위 = 궁금증형 제목이 상위권에 몰림. 단 '관찰'이지 인과 아님.
- **사용자 결정 A+C**: (A) 카드 순화 배포 — 제목에 "(참고용)" 붙이고, 상위 박스 강조색 제거, 캡션에 "상위권 N작(상·하위 각 K작) 표본 작음·순위 교란·인과 아님" 명시. (C) 아카 글엔 평균 숫자 대신 위 정성 관찰만 정직하게 쓸 것. 통계 주장 금지.
- B(조회수 더 수집)는 보류 — 개별작 스크래핑 부하 커서 지금 안 함.

### 후속 수정 배포(5b71494)
- ①휴리스틱 점수는 저장 안 함(페이지 "AI 채점" 표기와 어긋나는 데이터 방지). ②주간 리포트 메일은 정기 Vercel 크론에서만 자동 발송(수동 트리거 중복 방지 — 검증 중 주간 메일 1통 중복 발송된 것이 계기).

### 프로젝트 폴더 이동 확정 (사용자 결정)
- 새 경로: `C:\Users\asm04\OneDrive\바탕 화면\AI창업\novelmetric` (사용자가 직접 이동, 이전 경로 폐기 지시).
- 이동 시 node_modules 유실 → npm install 재실행(416 packages), 빌드 통과, 메일 감시·디시 추적 새 경로에서 정상 확인. 자동 작업 명령의 WorkingDirectory 전부 새 경로로 교체.
- 주의: 다음에 Claude Code를 열 때는 새 폴더에서 열 것. OneDrive 동기화 폴더라 .env.local이 클라우드에 올라감(사용자 인지 하에 확정).

### 매니저 메일 감시 종료 (7/20 사용자 지시)
- 웹연갤 매니저 답장 자동 감시(20분 주기) 중단 — 게시 허가·감사 회신까지 완료된 스레드라 더 볼 필요 없음. 스크립트(check-manager-reply.mjs)는 보존, 필요 시 "매니저 메일 감시 다시 켜줘"로 재가동.

### 아카라이브 데이터 글 게시 완료 + 반응 추적 가동 (7/20)
- **글 주소**: https://arca.live/b/webfiction/177401493 (웹소설 연재 채널, 사용자 직접 게시, 존댓말 최종본)
- **반응 추적**: 30분마다 `scripts/track-arca-post.mjs` (조회·댓글수·댓글 내용 파싱, arca-post-metrics.json 누적, cron 18,48분). 게시 직후 조회 7.
- **댓글 대응 위임(사용자 지시)**: 새 댓글에 답글 — 항상 존댓말, 팩트체크 후 사실만, 벤더명 금지.
- **아카 자동 로그인 시도 → 보류 (7/20)**: 계정(asm041019, 메모리 arca-credentials.md에 저장)으로 Playwright 로그인 구축했으나 Cloudflare 캡차가 자동화 브라우저 지문을 차단(사람 클릭도 무한 검증). 사용자 지시로 감시+알림 체제로 확정: 새 댓글 → 알림 + 답글 완성본 클립보드 복사 → 사용자가 붙여넣기. 스크립트 scripts/arca-session.mjs는 향후 재시도용으로 보존.
- 세션 종료 시 예약 소멸 — 재가동은 "아카 글 추적 다시 켜줘".

### 아카라이브 주간 데이터 글 준비 완료 (로드맵 ② 첫 편)
- 7/20 /insights 실데이터 기반 글 작성: 회귀 4%뿐 / 클리셰 없는 제목이 조회수 높음(7/13·7/20 동일 방향) / "되었다" 공식 / 노벨피아 차이 / 일주일 버틴 제목 3작.
- 원문: `scripts/arca-post-2026-07-20.txt` (제목+본문). 본문은 클립보드에 복사해 두고 글쓰기 창 열어둠 — 사용자가 붙여넣기+제출만 하면 됨. 게시는 사용자 직접(상시 원칙).
- 채널 정정: webnovel 채널은 비활성(구독자 0)이라 **장르소설 채널(arca.live/b/novelchannel, 구독자 2.7만)**로 확정. 말머리 '정보/칼럼' 권장. 크롬으로 글쓰기 창 열어둠.

### 아카 글 첫 반응 (08:20 KST) — 댓글 4개 도착
- 조회 70(1시간 새 +63) · 추천 4 · 댓글 4. 텍스트 댓글 2(겨울성 감사 인사 / 유저템프 "제목 후킹은 내용이 받쳐줘야, 결국 연독" 의견), 이모지 댓글 2(바나나나우유·싸탄).
- **답글 규칙 추가(사용자 지시)**: 이모지·이미지만 있는 댓글(text 빈 값)에는 답글 안 담. 문자 댓글에만 존댓말·팩트 답글. (메모리 community-post-polite-tone.md에 기록)
- 답글 초안 2건 작성해 전달(유저템프 건 클립보드 복사). 게시는 사용자 크롬에서 붙여넣기.

### 디시 홍보글 누적 수치 (08:20 KST)
- 조회 301 · 추천 6 · 댓글 5 (새 댓글 없음 — 완만한 유입 지속)

## 2026-07-19

### 재방문 유도 개선 배포 (C안: 사이트 훅 강화 + 이메일 부드러운 권유) — 커밋 `44a0d43`
- 점검 결과 문제 3가지: ①알림 임계값(선작+50/일 등)이 소형 작가에겐 도달 불가 → 알림이 영원히 안 옴 ②이메일 기본값 "안 받음" → 무료 유저 대부분 사이트 밖에서 부를 수단 없음 ③정기 메일은 Pro만.
- 수정: `lib/alerts.ts` 임계값을 작품 규모 비례로(최소 선작+5·조회+500·추천+10부터 감지), 선작 이정표 50/100/200/500/1000/3000 돌파 알림 추가. 대시보드 추적 완료 박스에 "다음 수집은 매일 새벽 3시" 재방문 훅 + 알림 미설정 유저용 이메일 나중 등록(EmailNudge, 같은 작품 upsert) 추가.
- 빌드 통과·벤더명 grep 0건·프로덕션 배포·라이브 청크에서 새 문구 확인 완료 (dpl_ATXJzKZdF4wJPweaCFj4UFC8iknU).
- 의도적으로 안 한 것: 무료 유저 정기(주간) 메일 — Pro 차별점 유지 위해 보류. 변화 있는 날만 발송하는 알림은 무료도 받음.

## 2026-07-18

### 디시 홍보글 누적 수치 (게시 후 ~1일)
- 조회 158 · 추천 4 · 댓글 5 (호평 2 + 지적 1 + 우리 답글 2). 유입 완만·안정 국면. 새 댓글 없음.

### 디시 웹연갤 홍보글 게시 완료 (00:48)
- **글 주소**: https://gall.dcinside.com/mgallery/board/view/?id=tgijjdd&no=1294715
- 닉네임 노블메트릭 / 비밀번호는 메모리(dc-post-credentials.md)에 저장. 말머리 일반, 정중한 말투 최종본, 서두 허락 명시.
- 게시 직후 초기값: 조회 4 → 2분 뒤 6. 추천 0, 댓글 0.
- **반응 추적 가동**: 30분마다 `node scripts/track-dc-post.mjs` (조회·추천·댓글 파싱, scripts/dc-post-metrics.json 누적). 댓글 달리면 푸시 알림+답변 초안 제안(답글 게시는 사용자 직접). 세션 종료 시 예약 소멸 — 재가동은 "디시 글 추적 다시 켜줘".

### 댓글 피드백 → 수정 → 배포 → 답글 (02:14 완료)
- 댓글 2개: ①"유용하다" 호평 ②"베스트 평균 말이 안 됨" 지적.
- 검증 결과 수치는 정확(투베 TOP10 누적 총조회수 평균) — 표기가 화당 조회수로 오해 유발.
- 수정: "누적 기준" 명시 + 투베 화당 평균·내 화당 평균 표시 (`122bca9`). 사용자 승인 후 프로덕션 배포, 라이브 API에서 avgViewsPerEp(≈1.85만) 확인.
- 사용자 위임으로 답글 직접 게시 완료 (02:14, "노블메트릭" 명의) — 인정+수정 완료 보고 내용.

### Google Search Console 등록 완료 (03:10)
- 속성 https://novelmetric.vercel.app (URL 접두어), HTML 파일 인증(public/googledbb374ccbee8ec7f.html — 삭제 금지) → 소유권 확인 성공.
- sitemap.xml 제출 완료 (직후 "가져올 수 없음" 표시는 정상, 수 시간 내 자동 처리). sitemap 자체는 200 응답 검증.
- /insights 수동 색인 요청은 일일 할당량 초과로 거부 — 내일 이후 가능하나 sitemap 크롤링으로 자동 진행되므로 필수 아님.
- GSC 대시보드: https://search.google.com/search-console?resource_id=https%3A%2F%2Fnovelmetric.vercel.app%2F (songminan90 구글 계정)

### 사용자 상시 지시 (2026-07-18, 07-20 추가)
- **커뮤니티(디시·아카) 게시글·댓글은 항상 존댓말(정중한 말투)로 작성** (2026-07-20 사용자 규칙 — 반말·디시체 초안 금지)
- **디시 글 탭은 항상 열어둘 것** (https://gall.dcinside.com/mgallery/board/view/?id=tgijjdd&no=1294715). 브라우저(Playwright) 연결이 살아있을 때는 이 탭을 열고 유지. 연결이 끊긴 동안엔 불가하므로 재연결 시 즉시 재개.
- **자동 작업 실행 시 검은 콘솔 창 뜨지 않게 할 것** (2026-07-19 사용자 요청). node를 숨김 창으로 실행: `Start-Process -FilePath "node" -ArgumentList "scripts\<script>.mjs [args]" -WorkingDirectory "C:\Users\asm04\OneDrive\바탕 화면\AI창업\novelmetric" -WindowStyle Hidden -Wait -RedirectStandardOutput $env:TEMP\nm-out.json; Get-Content $env:TEMP\nm-out.json -Raw`. 디시 추적·메일 감시 둘 다 이 방식으로. (직접 `node scripts\...` 호출하면 창이 뜸)

### 합의된 로드맵 (2026-07-18 사용자 승인 — "네 플랜대로 가자")
> 핵심 원칙: 지금 병목은 "노출"이 아니라 "전환 재료(쌓인 데이터)". 트래픽을 더 붓는 것보다 시간이 필요. 새 홍보글 남발 금지(업체 바이럴로 찍힘). 단발 홍보 → 데이터 콘텐츠로 전환.

**① 이번 주(~7/20): 관리만, 신규 홍보 금지**
- 디시/아카 기존 글 반응 관리(자동 추적 계속). 새 채널 확장 안 함.
- 디시 웹연갤은 상업광고 1회 소진 — 재게시 금지(규정 위반). 한 달 뒤 "정보 공지" 1회만 가능.

**② 다음 주(7/21~): 데이터 꾸준글 시작**
- 아카라이브에 /insights 데이터 인용 글 주 1편 ("이번 주 투베 제목 패턴 — 회귀 N%, 아카데미 N%"). 광고 아닌 정보 글. 초안은 Claude가 데이터 뽑아 작성, 게시는 사용자 직접.

**③ 8월 초: Pro 전환 국면**
- 추적 유저 데이터 2주치 쌓인 시점 → 주간 성장 리포트 이메일 발송 시작(이미 구현). 대기자 안내 메일 발송(scripts/send-waitlist-email.mjs, 본문 준비됨, dry_run 기본). 이때부터 조아라·트위터 등 새 채널 확장 최적기("2주 데이터 쌓임" 스토리 생김).

**④ 9월 초: 성과 판정**
- 8주 내 유료 5명 미만이면 가격(9,900→4,900원)/타겟(지망생→기성) 피벗 (LAUNCH.md §4). 4주차(8월 초) 중간 점검 1회.

**상시**
- 반응 추적 30분 주기(fetch 기반, 브라우저 불필요). 새 댓글 시 알림+답변 초안.
- GSC 색인 진행 관찰(등록 완료, 검색 유입은 며칠~2주 소요). 1~2주 뒤 GSC 실적 탭 확인.
- 브라우저(Playwright) 재연결 시: 디시 글 탭 상시 열기 + Vercel Analytics/Supabase 확인 재개.

## 2026-07-25 (금) — 출시 전 점검 + 페이앱 전자계약 완료

### 상태 점검 (실측)
- Vercel 7일: 방문자 244(-1%), PV 668(-10%), 이탈률 37%(개선). 페이지: / 107 · /insights 71 · 7/20인사이트 25 · /best 23 · /pro 13. 구글 검색 유입 9명 시작(GSC 효과).
- Supabase: 대기자 10명(전원 source=landing, diagnose 0), 추적 이메일 7명·추적 작품 20개, 중복 제거 전체 16명(+2).
- 체험코드 10개 DB 등록 완료(7/24) — gate API로 1번·10번 코드 유효(9/30까지)·가짜 코드 거부 검증.

### 페이앱 전자계약 (정산용)
- 네이버 메일로 온 (주)유디아이디 전자계약서 = 정품 확인(payapp.kr footer 사업자 113-86-56537 일치). 표준 약관 수준, 독소조항 없음.
- **사용자가 직접 서명 완료(7/25)**. 심사 3~4영업일 → ~7/29경 정산 가능 예상. 정산금 10만원 미만은 이월(9,900원 × 약 11건부터 첫 입금).
- 계약 특칙 리스크 차단: 휴대전화 결제 '수납률 99%' 조항 → `lib/payapp.ts`에 `openpaytype: card,kakaopay,naverpay,tosspay,payco` 추가(휴대전화 제외). 빌드·tsc·eslint 통과, prod 배포(Ready), 라이브 결제창에서 "휴대전화/phone" 0건 확인.
- Supabase 대시보드 세션 만료 → GitHub OAuth 클릭(비밀번호 입력 없음)으로 재로그인.

### 다음: 일요일 아카 글 초안(정성 관찰만, UTM 링크) → 월요일 발사(런북 scripts/LAUNCH-RUNBOOK-0727.md)
